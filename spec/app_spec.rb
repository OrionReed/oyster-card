# require 'app'

# describe App do
#   it { is_expected.to(respond_to(:run)) }
#   it { is_expected.to(respond_to(:startup)) }
#   it { is_expected.to(respond_to(:main_loop)) }
#   it { is_expected.to(respond_to(:balance)) }
#   it { is_expected.to(respond_to(:top_up)) }
#   it { is_expected.to(respond_to(:stations)) }
#   it { is_expected.to(respond_to(:history)) }
#   it { is_expected.to(respond_to(:start_journey)) }
#   it { is_expected.to(respond_to(:quit)) }
# end
